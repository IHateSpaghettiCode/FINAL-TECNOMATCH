{
  "builds": [
    {
      "dir": ".",
      "installCommand": "cd frontend && npm ci && cd ../Nuevo_back && npm ci",
      "buildCommand": "cd frontend && npm run build && rm -rf ../Nuevo_back/dist && cp -r dist ../Nuevo_back/dist",
      "startCommand": "./start.sh"
    }
  ],
  "plugins": [
    {
      "name": "node",
      "version": "18.x"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}

